import { TellersDirective } from './tellers.directive';

describe('TellersDirective', () => {
  it('should create an instance', () => {
    const directive = new TellersDirective();
    expect(directive).toBeTruthy();
  });
});
