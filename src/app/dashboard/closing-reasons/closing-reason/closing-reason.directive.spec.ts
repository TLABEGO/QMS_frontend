import { ClosingReasonDirective } from './closing-reason.directive';

describe('ClosingReasonDirective', () => {
  it('should create an instance', () => {
    const directive = new ClosingReasonDirective();
    expect(directive).toBeTruthy();
  });
});
