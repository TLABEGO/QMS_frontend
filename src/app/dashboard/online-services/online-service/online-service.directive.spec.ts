import { OnlineServiceDirective } from './online-service.directive';

describe('OnlineServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlineServiceDirective();
    expect(directive).toBeTruthy();
  });
});
