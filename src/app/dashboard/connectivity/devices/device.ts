import {Branch} from '../../branches/branch/branch';

export class DeviceConnectivity {

  branch: Branch;
  createDate: Date;
  deviceId: string;
  type: string;
  report: string;
  id: string;
  lastUpdate: Date;
}
