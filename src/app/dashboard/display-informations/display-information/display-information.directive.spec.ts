import { DisplayInformationDirective } from './display-information.directive';

describe('DisplayInformationDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayInformationDirective();
    expect(directive).toBeTruthy();
  });
});
