import { TicketDirective } from './ticket.directive';

describe('TicketDirective', () => {
  it('should create an instance', () => {
    const directive = new TicketDirective();
    expect(directive).toBeTruthy();
  });
});
