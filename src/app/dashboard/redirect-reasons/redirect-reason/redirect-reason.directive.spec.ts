import { RedirectReasonDirective } from './redirect-reason.directive';

describe('RedirectReasonDirective', () => {
  it('should create an instance', () => {
    const directive = new RedirectReasonDirective();
    expect(directive).toBeTruthy();
  });
});
