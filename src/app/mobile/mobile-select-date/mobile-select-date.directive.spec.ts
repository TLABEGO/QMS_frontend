import { MobileSelectDateDirective } from './mobile-select-date.directive';

describe('MobileSelectDateDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileSelectDateDirective();
    expect(directive).toBeTruthy();
  });
});
